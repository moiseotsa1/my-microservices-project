apiVersion: v1
kind: Service
metadata:
  name: postgres-replica
  namespace: microservices
spec:
  selector:
    app: postgres-read-replica
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
